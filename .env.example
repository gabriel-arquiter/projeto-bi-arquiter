# ─────────────────────────────────────────────────────────────
# Supabase — projeto Arquiter
# ─────────────────────────────────────────────────────────────

# Públicas (podem ir para o client — RLS protege a leitura)
NEXT_PUBLIC_SUPABASE_URL=https://zbtfjcgipjdvvxyozggj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=cole_a_anon_key_aqui

# PRIVADA — NUNCA expor no client. Só usada server-side (n8n / API routes que escrevem).
# O dashboard NÃO precisa dela para ler. Mantenha fora do client.
SUPABASE_SERVICE_ROLE_KEY=cole_a_service_role_key_aqui

# ─────────────────────────────────────────────────────────────
# Anthropic — agente IA (SOMENTE server-side, em /api/ai-agent)
# ─────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=cole_a_chave_anthropic_aqui

# Porta para o Hostinger Node.js hosting (geralmente injetada pelo painel)
PORT=3000
